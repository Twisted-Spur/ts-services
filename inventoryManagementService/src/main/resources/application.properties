spring.application.name=InventoryManagementService

spring.cloud.function.scan.packages=com.twistedspur.service

#http://localhost:8080/swagger-ui/index.html to access swagger

# Database connection properties
spring.datasource.url=jdbc:postgresql://twistedspur.postgres.database.azure.com:5432/twisted_spur
spring.datasource.username=tsAdmin
spring.datasource.password=G6IPzmy4gAihLH
spring.datasource.driver-class-name=org.postgresql.Driver
#spring.datasource.initialization-mode=always
#spring.sql.init.mode=always
#spring.sql.init.schema-locations=classpath:schema.sql
#spring.sql.init.data-locations=classpath:data.sql

# Hibernate properties
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# HikariCP settings (optional, but recommended for production)
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.pool-name=HikariPool
spring.datasource.hikari.max-lifetime=2000000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.leakDetectionThreshold=15000

# Optional: Hibernate DDL auto (none, validate, update, create, create-drop)
#spring.jpa.hibernate.ddl-auto=update

# Optional: Show SQL queries in the console
#spring.jpa.show-sql=true
